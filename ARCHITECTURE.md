# Architecture

The architecture documentation now lives in the guide so it is published with
the rest of the docs (single source of truth):

- **`src/architecture.md`** in this repository, or
- the **Architecture (for developers)** page of the published guide.

It covers the resolve → run → parse design, the four modules, the family map,
and where to make common changes.
